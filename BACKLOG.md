# SSHTool2 — 다음 버전 백로그

바로 릴리스하지 않고 다음 버전에 반영할 항목. 착수 시 이 목록에서 옮긴다.

## 대기 중
- [ ] **동시 명령 창 세션 수 미갱신** — 동시 명령(브로드캐스트) 창을 켠 상태에서 세션 개수가 바뀌어도 "대상 *개 세션" 표시가 업데이트되지 않음. 세션 열림/닫힘 시 카운트 재계산·표시 갱신 필요.
- [ ] **세션영역 키보드 탐색(탐색기 방식)** — 사이드바를 키보드로 조작. ↑/↓ 선택 이동, 폴더에서 ←=펼쳐져 있으면 접기·아니면 부모로 이동, →=접혀 있으면 펼치기·아니면 첫 자식으로, Enter=세션 접속/폴더 토글, Home/End=처음·끝. 최근접속 섹션 포함 여부 검토. 포커스 링·현재 항목 하이라이트 필요. (일반 탐색기/트리뷰 키 관례 준수)

## 배포 대기(검증 중단으로 홀드)
- [ ] **v0.33.1** — 한글 Ctrl+Enter 마지막 글자 밀림 3차 수정(비조합 Enter keydown 즉시 LF 경로까지 지연). 버전·빌드 완료, working tree 에 미커밋. 품질진단(이벤트 순서 검증) 재실행 후 배포.

## 완료(배포됨)
- [x] 중복 실행 방지/알림 (v0.31.0)
- [x] 한글 Ctrl+Enter 재수정·창 위치기억·F1 버전정보·사이드바 접기버튼 (v0.32.0)
- [x] **설정 백업 암호화 + ZIP + 폴더 정리** (v0.33.0)
  - 내보내기 암호화 바이너리(.stbak): PBKDF2-SHA512 300k + AES-256-GCM, 백업 암호 12자 이상(특수문자 강제 없음). 세션 비밀번호까지 담기므로 암호 없이는 열 수 없음.
  - 가져오기 암호 입력 + 구버전 평문 JSON 자동 판별(하위 호환).
  - 내보내기 시 [최신 앱(GitHub 릴리스 다운로드) + 암호화 백업] ZIP 옵션. 오프라인이면 앱 제외.
  - 설정 폴더를 %APPDATA%\SSHTool2 로 이전(구 폴더 보존, 완료 마커로 중단 시 재개).
