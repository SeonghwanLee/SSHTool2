# SSHTool2 — 다음 버전 백로그

바로 릴리스하지 않고 다음 버전에 반영할 항목. 착수 시 이 목록에서 옮긴다.

## 보안 — 착수 대기(설계·결정 확정됨)

검토 근거와 결정은 2026-07-27 논의 기준. 아래 순서대로 진행한다(뒤 항목이 앞 항목에 의존).

- [ ] **S0. `store.rs` 원자적 쓰기** — `store.rs:120,144` 가 `fs::write` 로 덮어써서, 쓰는 도중 크래시하면 세션 목록이 통째로 날아간다(`fs::write` 는 먼저 0바이트로 자른다). `vault.rs:172` 처럼 임시파일 + `rename` 으로 바꾼다. **S2·S3 의 선결 조건** — 이관 작업이 여기에 얹히면 유실 창이 커진다.
- [ ] **S1. 호스트키 첫 접속 프롬프트** — `hostkey.rs verify()` 의 `None` 분기가 지문을 보여주지도 않고 저장 후 통과시킨다(`ssh.rs:167`). 첫 접속 시점의 중간자가 그대로 성립하고 이후 그 키가 "정상"으로 굳는다. OpenSSH 처럼 SHA-256 지문을 보여주고 연결/취소를 묻는다. 두 번째부터의 Mismatch 처리는 이미 정상이라 첫 접속만 해당. ※ 레거시 알고리즘(SHA-1 KEX) 옵션과 겹치면 방어선이 둘 다 없어지므로 우선순위 높음.
- [ ] **S2. (D) 설정파일 암호화** — `sessions.json`·`settings.json` 을 AES-256-GCM 으로 암호화.
  - 키: 랜덤 32B 를 `keystore.rs` 에 항목 추가(`ACCOUNT = "file-key"`)해 OS 자격증명 저장소에 보관. `keyring` 이 이미 의존성이라 새 크레이트 불필요.
  - 새 모듈 `filecrypt.rs`, 스킴은 `vault.rs` 와 동일(nonce 12B 선행). 파일 앞에 매직 헤더(`STB2`+버전) 를 붙여 평문과 구분 → `load` 에서 매직 없으면 평문으로 읽고 다음 `save` 에서 자동 이관.
  - **결정: 키체인 실패 시 평문 폴백 — 단, 다운그레이드 방지 조건을 붙인다.** 이미 암호화된 파일이 있는데 키를 못 읽으면 평문으로 덮어쓰지 말고 저장을 거부·알림. 평문 폴백은 한 번도 암호화된 적 없는 최초 상태에서만. (조건이 없으면 공격자가 키체인 항목만 지워서 암호화를 끌 수 있다.)
  - **필수 동반 수정: `backup.rs`** — 번들이 파일을 `BTreeMap<String, String>`(UTF-8 텍스트)로 담아(`backup.rs:82`) 바이너리를 못 넣는다. 게다가 파일 키는 PC마다 달라 암호화된 채 넣으면 다른 PC 에서 복원 불가 → 내보낼 때 복호화, 복원할 때 그 PC 키로 재암호화.
  - 한계(안내 문구에 반영): 키가 OS 자격증명 저장소에 있으므로 **같은 Windows 계정으로 실행되는 코드는 그대로 읽는다.** D 가 막는 것은 파일이 계정 밖으로 나가는 경로(백업 SW·로밍 프로필·공유 폴더·USB·폴더째 전달)까지다.
- [ ] **S3. (C) 민감 필드를 볼트로** — 대상은 `triggers[].send` 와 `startupCommands`.
  - 볼트 `entries` 키에 네임스페이스: `"{id}"`=비밀번호(기존 그대로), `"{id}:triggers"`, `"{id}:startup"`. 기존 키는 UUID 라 `:` 충돌 없음. `set_password`/`get_password` 는 범용 `set_secret`/`get_secret` 의 얇은 래퍼로.
  - **결정: 트리거별 '비밀 값' 체크박스.** 체크된 트리거만 볼트로 보내고 그때만 볼트 설정을 요구한다. `y`/`q` 같은 무해한 트리거는 볼트 없이 그대로 쓴다. (`send` 값이 있으면 무조건 볼트로 강제하면 볼트를 안 쓰는 사용자가 무해한 트리거까지 못 쓴다.)
  - **결정: 기존 평문 값은 볼트가 처음 해제되는 시점에 조용히 자동 이관.** 순서는 볼트에 쓰기 → 파일에서 제거(반대로 하면 유실). S0 선행 필수.
  - 잠긴 상태에서도 사이드바 목록은 그대로 보인다(호스트·계정은 평문 유지). 접속할 때만 해제 요구 — 저장된 비밀번호와 같은 흐름(`main.ts:285` `ensureVaultUnlocked()`).
- [ ] **S4. 트리거 자동 전송 위험 안내** — `tabs.ts checkTriggers` 는 **서버가 보낸 출력**에 반응해 자동 전송한다. 트리거에 sudo 비밀번호를 넣으면 **서버가 그 프롬프트 문자열을 아무 때나 출력해 받아낼 수 있다.** `stripAnsi` 후 매칭이라 색상 코드 위장도 통하고 쿨다운이 1초라 반복 수집도 가능. S3(볼트 저장)는 디스크만 지킬 뿐 이 경로는 그대로다 — "볼트에 넣었으니 안전"이라는 오해가 생기지 않도록 트리거 편집 UI 에 경고를 넣는다. 발동을 접속 직후 일정 시간으로 제한하는 안도 같이 검토.
- [ ] **S5. CSP 설정** — `tauri.conf.json:24` 가 `"csp": null`. 원격 콘텐츠를 안 불러와 당장의 공격면은 좁지만, 비밀번호가 이미 WebView JS 힙에 올라오고 S3 이후엔 트리거 값까지 올라온다.

### 안내 문구에 반드시 반영할 한계
- 이관 후에도 **디스크의 평문 흔적은 남는다** — 덮어쓰기·삭제로는 원래 섹터가 지워지지 않고, SSD wear-leveling 때문에 확실한 삭제가 불가능하다. 이관 전에 만든 `.stbak` 백업에도 평문 트리거가 들어 있다. "이관하면 과거 흔적까지 사라진다"고 안내하면 안 된다.
- 접속 중에는 볼트에서 꺼낸 값이 **WebView JS 힙에 평문으로 상주**한다. JS 에서는 확실한 메모리 소거가 불가능하다.

## 대기 중
- [ ] **자격증명 입력창을 접속 완료 이후로** — 지금은 세션을 열자마자(네트워크 접속 전) 비밀번호 창이 뜬다. 실제 접속(TCP+키교환)이 끝난 뒤에 띄울 것. 아이디가 저장된 세션은 저장된 아이디로 먼저 인증을 시작하고, 서버 응답이 온 다음에 비밀번호 창을 띄운다.
- [ ] **세션영역 하단 검은 영역** — 사이드바 아래쪽에 사이드바와 같은 너비의 검은 띠가 보임. 화면 글자 크기를 확대/축소하면 보였다 안 보였다 함. WPF 에서 잡았던 증상이 웹(Tauri)으로 넘어오며 재발한 것으로 보임.
  - 조사 결과: 헤드리스 Chromium 으로 zoom 0.67~1.5 를 훑어도 사이드바 폭의 검은 띠는 재현되지 않았다. `#session-tree` 는 `overflow-x: hidden` 이라 가로 스크롤바는 나올 수 없고, `#sidebar` 는 그리드 행에 stretch 되어 아래가 비지 않는다.
  - 다만 `#app { height: 100vh }` 는 CSS zoom 과 어긋나 앱 전체가 뷰포트보다 짧아지는 조건이 있었다(전폭 띠). 실제 WebView2 의 Ctrl+± 줌은 동작이 달라 이게 원인이라 단정할 수 없음.
  - 남은 확인: 스크린샷, 띠가 나타날 때의 사이드바 폭·줌 배율.

## 구현 완료(버전 미할당 · 배포 대기)
- [x] **동시 명령 창 세션 수 갱신** — `TabManager.onTabsChanged()` 구독을 추가해 탭 열림/닫힘·접속 상태 변화 시 "대상 *개 세션" 표시를 다시 계산한다.
- [x] **세션영역 키보드 탐색(탐색기 방식)** — roving tabindex 로 ↑/↓·←/→·Enter·Home/End 지원. 최근 접속 섹션도 같은 순서에 포함(별도 구획으로 빼지 않음 — 위아래 이동이 한 줄기로 이어지는 편이 트리뷰 관례에 가깝다고 판단). 포커스 링 CSS 추가.
- [x] **구형 서버 접속(CentOS 5 등)** — 세션별 '구형 서버 호환(레거시 알고리즘 허용)' 옵션. 켜면 SHA-1 KEX(gex-sha1·group14-sha1·group1-sha1)·hmac-sha1·AES-CBC 를 협상 목록 *맨 뒤* 에 덧붙인다. 순서가 뒤이므로 최신 서버 접속 결과는 그대로다. 3des-cbc 는 제외(russh `des` 피처 필요 + 64비트 블록으로 AES-CBC 보다 약함). 옵션이 꺼진 상태에서 알고리즘 불일치로 끊기면 오류 메시지가 이 옵션을 안내한다.
- [x] **상태바 세션별 접속 유지시간** — 하단 정보바에 `⏱ MM:SS`(1시간 넘으면 `HH:MM:SS`) 표시. 기준은 셸이 열린 시점, 끊기면 최종값에서 멈추고 회색으로 흐려진다. 로컬 셸도 동일. 탭 전환은 기존 `activate()→layout()→emitStatus()` 경로가 그대로 처리한다. 1초 타이머는 활성 탭이 접속 유지 중일 때만 emit 한다.
- [x] **접기 버튼이 터미널 침범** — 4px 폭조절바 위에 18px 버튼을 띄워 남는 폭이 터미널을 덮었다. 접힌 상태에서만 트랙을 18px 로 넓히고 버튼을 왼쪽에 붙여 겹침을 없앴다(측정: 버튼 0~18px, 작업영역 시작 18px).

## 완료(배포됨)
- [x] **한글 Ctrl+Enter 마지막 글자 밀림 3차 수정** (v0.33.1) — 비조합 Enter keydown 즉시 LF 경로까지 지연.
- [x] 중복 실행 방지/알림 (v0.31.0)
- [x] 한글 Ctrl+Enter 재수정·창 위치기억·F1 버전정보·사이드바 접기버튼 (v0.32.0)
- [x] **설정 백업 암호화 + ZIP + 폴더 정리** (v0.33.0)
  - 내보내기 암호화 바이너리(.stbak): PBKDF2-SHA512 300k + AES-256-GCM, 백업 암호 12자 이상(특수문자 강제 없음). 세션 비밀번호까지 담기므로 암호 없이는 열 수 없음.
  - 가져오기 암호 입력 + 구버전 평문 JSON 자동 판별(하위 호환).
  - 내보내기 시 [최신 앱(GitHub 릴리스 다운로드) + 암호화 백업] ZIP 옵션. 오프라인이면 앱 제외.
  - 설정 폴더를 %APPDATA%\SSHTool2 로 이전(구 폴더 보존, 완료 마커로 중단 시 재개).
