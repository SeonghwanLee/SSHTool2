# SSHTool2 — 다음 버전 백로그

바로 릴리스하지 않고 다음 버전에 반영할 항목. 착수 시 이 목록에서 옮긴다.

## 대기 중
- (없음)

## 완료
- [x] **중복 실행 방지/알림** (v0.31.0) — `tauri-plugin-single-instance` 로 두 번째 실행 시 기존 창을 앞으로(unminimize+show+focus) 가져오고, `second-instance` 이벤트로 "이미 실행 중입니다" 토스트 표시.
