# SSHTool2 — 다음 버전 백로그

바로 릴리스하지 않고 다음 버전에 반영할 항목. 착수 시 이 목록에서 옮긴다.

## 보안 — S0·S1(v0.34.0), S2·S4·S5(v0.35.0), S3(v0.36.0) 완료 · S4-b 만 검토 대기

검토 근거와 결정은 2026-07-27 논의 기준. 아래 순서대로 진행한다(뒤 항목이 앞 항목에 의존).

- [x] **S0. `store.rs` 원자적 쓰기** (v0.34.0) — `store.rs:120,144` 가 `fs::write` 로 덮어써서, 쓰는 도중 크래시하면 세션 목록이 통째로 날아간다(`fs::write` 는 먼저 0바이트로 자른다). `vault.rs:172` 처럼 임시파일 + `rename` 으로 바꾼다. **S2·S3 의 선결 조건** — 이관 작업이 여기에 얹히면 유실 창이 커진다.
- [x] **S1. 호스트키 첫 접속 프롬프트** (v0.34.0) — `hostkey.rs verify()` 의 `None` 분기가 지문을 보여주지도 않고 저장 후 통과시킨다(`ssh.rs:167`). 첫 접속 시점의 중간자가 그대로 성립하고 이후 그 키가 "정상"으로 굳는다. OpenSSH 처럼 SHA-256 지문을 보여주고 연결/취소를 묻는다. 두 번째부터의 Mismatch 처리는 이미 정상이라 첫 접속만 해당. ※ 레거시 알고리즘(SHA-1 KEX) 옵션과 겹치면 방어선이 둘 다 없어지므로 우선순위 높음.
- [x] **S2. (D) 설정파일 암호화** (v0.35.0) — 아래 완료 항목 참조.
- [x] **S3. (C) 민감 필드를 볼트로** (v0.36.0) — 아래 완료 항목 참조.
- [x] **S4. 트리거 자동 전송 위험 안내** (v0.35.0) — 아래 완료 항목 참조.
- [x] **S4-b. 트리거 발동 시간 제한** (v0.37.0) — 접속 후 10초 안에만 발동한다.
  - 검토 단계에서는 "실효가 작다"고 봤다(서버를 장악한 쪽은 출력 시점을 스스로 골라 접속 직후에 패턴을 뱉으면 그만이고, 값은 한 번만 빼내면 된다). 사용자 판단으로 적용했다.
  - 검토 때 지적한 부작용("한참 뒤 `su -` 가 조용히 안 먹고 원인도 못 찾는다")은 안내로 덮었다 — 창이 지난 뒤 규칙이 걸리면 터미널에 한 번 알린다(세션당 1회, 재접속 시 초기화).
  - 기준 시점은 셸이 열린 때(`connectedAt`, 유지시간 표시와 같은 값). 재접속하면 창이 다시 열린다.

## 대기 중
- [x] **자격증명 입력창을 접속 완료 이후로** (v0.37.0) — 프롬프트 직전에 `ssh_probe` 로 서버에 먼저 붙어 본다. TCP·키교환·호스트키 확인까지 마치고 `authenticate_none` 으로 계정을 알려 서버 응답까지 받은 뒤에 창을 띄운다. 실패하면 비밀번호를 묻지 않고 사유를 그대로 보여 준다.
  - ⚠ 애초 구상(인증 흐름을 `ssh_open` + `ssh_authenticate` 로 쪼개 백엔드가 자격증명을 되묻는 구조)은 채택하지 않았다. 접속 경로 전체를 갈아엎는 데다 이 장비에서 Rust 컴파일 검증이 불가해 위험이 컸다. probe 방식은 기존 접속 경로를 건드리지 않으면서 같은 요구(접속 확인 후 · 서버 응답 후 프롬프트)를 만족한다.
  - 비용: 접속 때 TCP 연결이 한 번 더 생긴다(probe → 끊기 → 실제 접속). 저장된 비밀번호가 있으면 probe 없이 바로 접속하므로 평소 경로에는 영향이 없다.
- [x] **SFTP 전송 중 실수로 닫힘 방지** (v0.37.0) — 전송 중에는 바깥 클릭을 무시하고, 닫기 버튼은 확인을 거친다. 파일 자체는 이미 `.part` 로 받아 취소해도 안전했지만(대상 파일 보존·부분 파일 삭제) 받던 시간이 날아가는 것은 막지 못했다.
## 완료(v0.36.0)
- [x] **S3. 민감 필드를 볼트로** — `triggers[].send` 와 `startupCommands` 를 **옵트인**으로 볼트에 보관한다.
  - **결정 (B) 채택: 둘 다 '비밀 값' 체크박스.** 체크한 것만 볼트로 가고 그때만 볼트 설정을 요구한다. 무조건 볼트로 보내면 `cd /projects` 만 쓰는 세션도 접속마다 볼트 해제를 요구받는다.
  - **부수 효과 — 자동 이관이 필요 없어졌다.** 기본값이 꺼짐이라 기존 값은 그대로 파일에 남고, 사용자가 체크해 저장하는 그 순간에만 볼트로 옮겨진다. 애초 스펙의 "볼트 첫 해제 시 조용히 자동 이관"은 (B) 에서는 대상이 없어 구현하지 않았다 — 조용한 일괄 이관의 위험(사용자 모르게 접속 조건이 바뀜)도 함께 사라졌다.
  - 볼트 `entries` 키 네임스페이스: `"{id}"`=비밀번호(기존 그대로), `"{id}:triggers"`(비밀 규칙의 send 만 배열로), `"{id}:startup"`. `set_password`/`get_password` 는 `set_secret`/`get_secret` 의 얇은 래퍼가 됐다.
  - 트리거는 `pattern` 은 파일에 남기고 `send` 만 볼트로 — 패턴은 비밀이 아니고, 파일만 보고도 규칙 개수·순서를 알 수 있어야 복원이 맞는다.
  - 저장 시 `extractSecrets()`, 접속·편집 시 `hydrateSecrets()`. 편집 창은 볼트 값을 채워서 연다(빈 칸으로 열리면 지워진 걸로 오해한다). 볼트 해제를 취소하면 **저장 자체를 중단**한다 — 그냥 진행하면 값이 평문으로 파일에 남는다.
  - 세션 삭제 시 `delete_password` 가 `{id}` 와 `{id}:*` 를 함께 지운다(유령 항목 방지). 복제본은 id 가 달라 원본 볼트 항목을 못 가리키므로 비밀 플래그를 끄고 값도 비운다.
  - ⚠ 볼트 저장은 **디스크에 남는 값만** 가린다. S4 가 경고하는 자동 전송 유출 경로는 그대로다 — 경고 문구에 이 점을 명시했다.

## 완료(v0.35.0)
- [x] **S2. 설정파일 암호화** — 새 모듈 `filecrypt.rs`. `sessions.json`·`settings.json` 을 AES-256-GCM 으로 봉인하고, 랜덤 32B 키를 `keystore.rs` 의 `"file-key"` 항목(OS 자격증명 저장소)에 둔다. 파일 앞 매직 `STB2`+버전으로 평문/암호문을 구분해 구버전 파일은 그대로 읽고 다음 저장에서 자동 이관.
  - 다운그레이드 방지: 같은 폴더의 관리 대상 중 **하나라도 암호문인데** 키를 못 얻으면 저장을 거부한다. 한 파일만 보면 아직 암호화되지 않은 쪽으로 평문 저장이 열려 버려서 두 파일을 함께 본다. 평문 폴백은 한 번도 암호화된 적 없는 최초 상태에서만.
  - 프런트는 저장 실패를 실행당 한 번 알린다(`dialogs.ts saveFailureAlert`) — 저장은 자동으로 여러 번 일어나 매번 띄우면 앱을 쓸 수 없다.
  - `backup.rs`: 내보낼 때 `filecrypt::read_text` 로 복호화해 담고(읽기 실패는 조용히 건너뛰지 않고 중단 — 반쪽 백업 방지), 복원할 때 그 PC 키로 재암호화. 복원 직전 `clear_managed()` 로 기존 세션·설정 파일을 치운다 — 키를 잃어 읽을 수 없게 된 암호문이 남아 있으면 다운그레이드 방지 조건이 **복구 자체를 막는다**.
  - `factory_reset` 은 `"file-key"` 항목을 지우지 않는다(파일이 없어져 무해). 정리를 원하면 별도 판단 필요.
- [x] **S4. 트리거 자동 전송 위험 안내** — 트리거 편집 화면의 경고를 "값이 암호화되지 않는다"에서 **발동 조건**중심으로 바꿨다. 서버 출력에 반응한다는 점, 서버를 장악한 쪽이 패턴 문자열을 출력해 값을 끌어낼 수 있다는 점, 색상 코드 위장과 1초 반복 수집이 가능하다는 점을 명시.
- [x] **터미널 하단 검은 띠 (3차 만에 원인 확정)** — `xterm.css:93` 의 `.xterm .xterm-viewport { background-color: #000 }` 이 진짜 원인. 앱의 `.term-host .xterm-viewport` 는 특이도가 **같아서**(둘 다 0,2,0) 나중에 로드되는 xterm.css 가 이겼다. 0.29.0·0.30.0 은 배경색을 덧칠했지만 이 싸움에서 져 증상이 남았다. `.term-host .xterm .xterm-viewport` 로 0,3,0 을 만들어 로드 순서와 무관하게 이기도록 했다. 띠가 보이는 조건은 컨테이너 높이가 행 높이의 배수가 아닐 때 — viewport 가 screen 보다 그만큼 커서 자투리가 드러난다. 실측: 수정 전 `rgb(0,0,0)` → 수정 후 `rgb(45,53,59)`.
- [x] **SFTP 진입점 확대** — 최근 접속 행(호버 시 SFTP 칩 + 우클릭 메뉴)과 세션 탭 우클릭 메뉴에서 SFTP 를 연다. 자격증명 처리는 `main.ts openSftpFor()` 한 곳으로 모아 사이드바·탭이 공유한다. 노출 기준은 세션 행과 동일(로컬 셸·`enableSftp` 꺼짐이면 숨김) — 브라우저로 3가지 조합 검증 완료.
- [x] **한글 조합 표시** — 조합 오버레이 배경을 터미널 배경과 같게 두고 밑줄로만 표시. 0.28.0 이 xterm 기본(검정 박스)을 `--panel-2` 로 바꿨는데 그 색이 터미널 배경보다 밝아 회색 박스로 도드라졌다("0.3x 이전엔 괜찮았다"는 체감과 시점이 맞는다).
  - ⚠ 처음 의심한 '오버레이가 한 셀 밀림'은 **사실이 아니었다.** 두 번 측정 모두 커서 셀과 정확히 일치(`compositionLeft 98px = bufferX 14 × cellW 7`, `x=128 = 셀 16`). '조합 중 커서 숨기기'도 적용 전후 스크린샷이 픽셀 동일해 효과가 없었다. 두 안 모두 채택하지 않았다.
  - 사용자 확인 결과 bash 프롬프트에서는 증상이 없어, 스크린샷의 커서 블록 자체는 Claude CLI 가 자기 입력줄에 그리는 것으로 결론. 앱에서 고칠 대상이 아니다.
- [x] **S5. CSP 설정** — `default-src 'self'` 기반. `style-src` 만 `'unsafe-inline'` 을 남긴다(xterm 이 `<style>` 을 동적으로 만들고 앱 전반이 `style` 속성을 쓴다 — CSP 는 `style` 속성도 `style-src` 로 막는다). IPC 는 Windows 에서 `http://ipc.localhost` 로 `fetch` 하므로 `connect-src` 에 넣었다. 폰트·스크립트·CSS 는 모두 번들 자산이라 `'self'` 로 충분.
  - 검증: `dist/` 를 같은 CSP 헤더로 서빙하고 헤드리스 Chromium 으로 로드 → 위반 0건, 내장 폰트 로드·인라인 style 속성·동적 `<style>`·xterm 렌더 모두 정상. `'unsafe-inline'` 을 뺀 대조군에서는 `style-src-elem` 위반이 잡혀 검사 자체가 유효함을 확인.

## 완료(v0.34.0)
- [x] **동시 명령 창 세션 수 갱신** — `TabManager.onTabsChanged()` 구독을 추가해 탭 열림/닫힘·접속 상태 변화 시 "대상 *개 세션" 표시를 다시 계산한다.
- [x] **세션영역 키보드 탐색(탐색기 방식)** — roving tabindex 로 ↑/↓·←/→·Enter·Home/End 지원. 최근 접속 섹션도 같은 순서에 포함(별도 구획으로 빼지 않음 — 위아래 이동이 한 줄기로 이어지는 편이 트리뷰 관례에 가깝다고 판단). 포커스 링 CSS 추가.
- [x] **구형 서버 접속(CentOS 5 등)** — 세션별 '구형 서버 호환(레거시 알고리즘 허용)' 옵션. 켜면 SHA-1 KEX(gex-sha1·group14-sha1·group1-sha1)·hmac-sha1·AES-CBC 를 협상 목록 *맨 뒤* 에 덧붙인다. 순서가 뒤이므로 최신 서버 접속 결과는 그대로다. 3des-cbc 는 제외(russh `des` 피처 필요 + 64비트 블록으로 AES-CBC 보다 약함). 옵션이 꺼진 상태에서 알고리즘 불일치로 끊기면 오류 메시지가 이 옵션을 안내한다.
- [x] **상태바 세션별 접속 유지시간** — 하단 정보바에 `⏱ MM:SS`(1시간 넘으면 `HH:MM:SS`) 표시. 기준은 셸이 열린 시점, 끊기면 최종값에서 멈추고 회색으로 흐려진다. 로컬 셸도 동일. 탭 전환은 기존 `activate()→layout()→emitStatus()` 경로가 그대로 처리한다. 1초 타이머는 활성 탭이 접속 유지 중일 때만 emit 한다.
- [x] **접기 버튼이 터미널 침범** — 4px 폭조절바 위에 18px 버튼을 띄워 남는 폭이 터미널을 덮었다. 접힌 상태에서만 트랙을 18px 로 넓히고 버튼을 왼쪽에 붙여 겹침을 없앴다(측정: 버튼 0~18px, 작업영역 시작 18px).

## 완료(배포됨)
- [x] **한글 Ctrl+Enter 마지막 글자 밀림 3차 수정** (v0.33.1) — 비조합 Enter keydown 즉시 LF 경로까지 지연.
- [x] 중복 실행 방지/알림 (v0.31.0)
- [x] 한글 Ctrl+Enter 재수정·창 위치기억·F1 버전정보·사이드바 접기버튼 (v0.32.0)
- [x] **설정 백업 암호화 + ZIP + 폴더 정리** (v0.33.0)
  - 내보내기 암호화 바이너리(.stbak): PBKDF2-SHA512 300k + AES-256-GCM, 백업 암호 12자 이상(특수문자 강제 없음). 세션 비밀번호까지 담기므로 암호 없이는 열 수 없음.
  - 가져오기 암호 입력 + 구버전 평문 JSON 자동 판별(하위 호환).
  - 내보내기 시 [최신 앱(GitHub 릴리스 다운로드) + 암호화 백업] ZIP 옵션. 오프라인이면 앱 제외.
  - 설정 폴더를 %APPDATA%\SSHTool2 로 이전(구 폴더 보존, 완료 마커로 중단 시 재개).
