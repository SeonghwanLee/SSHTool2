# SSHTool2 — 다음 버전 백로그

바로 릴리스하지 않고 다음 버전에 반영할 항목. 착수 시 이 목록에서 옮긴다.

## 대기 중
- [ ] **중복 실행 방지/알림** — 앱이 이미 실행 중일 때 다시 실행하면, 새 인스턴스를 띄우지 않고 기존 창을 앞으로 가져오며 "이미 실행 중" 알림 표시.
  - 방식(안): Tauri `tauri-plugin-single-instance` 사용 → 두 번째 실행 시 콜백에서 기존 main window `unminimize()`+`set_focus()`, 필요 시 토스트/알림.
  - 확인 필요: NSIS(현재 사용자 설치)와의 궁합, 자동 업데이트 재시작 흐름에서 오탐 없는지.
